name: learn-github-actions
on: [push]
jobs:
  build-ionic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: |
          npm i
          npm run build
